import Button from "@/components/Button";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <h1>Welcome!</h1>
      <div>
        <Button>Hi</Button>
      </div>
    </div>
  );
};

export default Home;
